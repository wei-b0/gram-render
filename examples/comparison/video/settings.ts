/**
 * Composition constants — in a plain .ts module so both the root program
 * (render.ts) and the JSX program (Root.tsx) can import them.
 */

export const FPS = 30;
export const WIDTH = 1920;
export const HEIGHT = 1080;
